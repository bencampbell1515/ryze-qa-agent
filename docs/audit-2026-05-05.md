# Project Audit — 2026-05-05

## Executive Summary

- **Total findings:** 50 (3 critical, 13 high, 13 medium, 12 low, 9 info/confirmed-ok)
- **Domains audited:** Noise Filter Completeness, ATC/Revenue Check Robustness, Deduplication Fingerprint Correctness, Sitemap Discovery & robots.txt Compliance, Content Check Injection Safety, Bug Accumulation & Multi-Run Data Integrity, Playwright Concurrency & Listener Isolation
- **Rounds completed:** 2 (1 follow-up round for CRAWL-006)

---

## Domain: Noise Filter Completeness & Accuracy

### NOISE-001 — Shopify /t?event= analytics 404 not filtered — HIGH

> **Plain English:** CLAUDE.md documents that Shopify's analytics endpoint generates false-positive network:404 bugs, but no filter for it exists anywhere in the code. One deduplicated false-positive bug survives into every audit report.

The `/t?event=<base64-uuid>` URL pattern is documented in CLAUDE.md as requiring a filter in `scripts/report.ts`, but none exists. The endpoint is same-origin (not caught by NOISE_HOSTS) and not matched by NOISE_404_URL_PATTERNS. `normalizeMessage()` collapses all per-page instances into one fingerprint — but that single fingerprint still surfaces in the final report as an actionable-looking `network:404` bug.

**File:** `scripts/report.ts:51`
**Evidence:**
```ts
const NOISE_404_URL_PATTERNS = [
  /\/em-prerender/,
  /\/em-cgi\//,
  /cdn\.shopify\.com\/s\/files\/.*\/t\/(?!2676\/)[0-9]+\//,
  // No pattern for /t?event= exists here or anywhere in report.ts
];
```
**Verdict:** BUG

---

### NOISE-002 — 14 noise hosts missing from check-time filter — MEDIUM

> **Plain English:** Fourteen third-party hosts are only filtered at report-time, not when the Playwright event fires. Every audit run writes hundreds of noise entries for these hosts to bugs.jsonl before they are discarded, growing the file indefinitely.

`tests/checks/network.ts` NOISE_HOSTS has 13 entries; `scripts/report.ts` suppresses 14 additional hosts: `applovin.com`, `sentry.io`, `postscript.io`, `clarity.ms`, `mountain.com`, `launchdarkly.com`, `segment.com`, `amplitude.com`, `intercom.io`, `hotjar.com`, `zendesk.com`, `otlp-http-production.shopifysvc.com`, `id.ryzesuperfoods.com`, `api.rechargeapps.com`. These generate raw `network:403`/`network:4xx` entries in bugs.jsonl on every run.

**File:** `tests/checks/network.ts:5`
**Evidence:**
```ts
// network.ts NOISE_HOSTS ends at 'monorail-edge.shopifysvc.com'
// Missing: applovin.com, sentry.io, postscript.io, clarity.ms, mountain.com,
//          launchdarkly.com, segment.com, amplitude.com, intercom.io, hotjar.com,
//          zendesk.com, otlp-http-production.shopifysvc.com, id.ryzesuperfoods.com,
//          api.rechargeapps.com
```
**Verdict:** DESIGN_FLAW

---

### NOISE-003 — Edgemesh URL patterns asymmetric between filter layers — MEDIUM

> **Plain English:** The two filter layers know about different Edgemesh path patterns: check-time knows `/em-js/` but not `/em-prerender`; report-time knows `/em-prerender` but not `/em-js/`. The two layers are not maintained in sync.

`network.ts` NOISE_URL_PATTERNS: `['/em-cgi/', '/em-js/']`. `report.ts` NOISE_404_URL_PATTERNS: `[/\/em-prerender/, /\/em-cgi\//]`. The `/em-js/` pattern is exclusive to check-time; `/em-prerender` is exclusive to report-time. This asymmetry is currently benign but encodes different assumptions and will drift further without a single source of truth.

**File:** `tests/checks/network.ts:13`
**Evidence:**
```ts
// network.ts:
const NOISE_URL_PATTERNS = ['/em-cgi/', '/em-js/'];

// report.ts:
const NOISE_404_URL_PATTERNS = [
  /\/em-prerender/,   // not in network.ts
  /\/em-cgi\//,
  // /em-js/ is absent
];
```
**Verdict:** DESIGN_FLAW

---

### NOISE-004 — `.myshopify.com` absent from both NOISE_HOSTS lists — MEDIUM

> **Plain English:** CLAUDE.md lists ".myshopify.com requests after checkout handoff" as known noise, but neither filter layer contains `myshopify.com`. Cart/ATC flows can trigger these requests.

The ATC flow navigates to `/cart` (revenue.ts line 54), which triggers requests to `ryzesuperfoods.myshopify.com` endpoints. Neither `tests/checks/network.ts` nor `scripts/report.ts` NOISE_HOSTS includes `myshopify.com`. A single entry would suppress all subdomains via the `.endsWith()` check.

**File:** `null` (cross-cutting)
**Evidence:**
```
CLAUDE.md known noise: '.myshopify.com requests after checkout handoff'
grep -n 'myshopify' tests/checks/network.ts scripts/report.ts → 0 results
```
**Verdict:** BUG

---

### NOISE-005 — NOISE_404_URL_PATTERNS only applied to `network:404`, leaves `em-prerender` 403/5xx unfiltered — LOW

> **Plain English:** If an Edgemesh `/em-prerender` endpoint returns 403 instead of 404, the pattern check is skipped (it only runs for `network:404`) and the false positive survives into the report.

The `isNoise()` guard at line 65 is `if (inst.ruleId === 'network:404')`. A 403 to `/em-prerender` has `ruleId='network:403'`, skips this block entirely, and passes both filter layers since it is same-origin (not in NOISE_HOSTS).

**File:** `scripts/report.ts:65`
**Evidence:**
```ts
if (inst.ruleId === 'network:404') {
  if (NOISE_404_URL_PATTERNS.some((p) => p.test(inst.message))) return true;
}
// A network:403 for /em-prerender skips this block entirely.
```
**Verdict:** BUG

---

### NOISE-006 — CLAUDE.md documents specific message-pattern filters that do not exist in code — LOW

> **Plain English:** The docs say two specific regex patterns are filtered in `scripts/report.ts`, but neither exists. They're suppressed only because their entire rule category is blocked — a blunter mechanism that masks the gap.

CLAUDE.md "Message patterns filtered" section lists two regexes (`Requests are not allowed to the same origin.*\/t\?event=` and `Cannot read properties of null (reading 'addEventListener')`). Neither appears in `scripts/report.ts`. Suppression is achieved by `NOISE_RULE_IDS.has('js:pageerror')` and `NOISE_RULE_IDS.has('console:error')` — whole categories, not per-message. If those categories were ever split, the documented per-message filters would need to be written from scratch.

**File:** `CLAUDE.md:91`
**Evidence:**
```ts
// CLAUDE.md claims targeted per-message filtering exists.
// scripts/report.ts — no such patterns:
const NOISE_RULE_IDS = new Set([
  'console:error', // entire category, not per-message
  'js:pageerror',
]);
```
**Verdict:** BUG

---

### NOISE-007 — Shopify CDN inactive-theme regex correctly implemented — INFO

> **Plain English:** The regex that filters stale Shopify CDN assets correctly passes active theme t/2676 and rejects all other theme IDs including t/26760 (which could be a false prefix match).

The negative-lookahead `(?!2676\/)` anchors on the trailing slash, preventing the active theme ID from being a false prefix-match for IDs like `26760`.

**File:** `scripts/report.ts:58`
**Evidence:** Pattern correctly tested: `t/2676/` → not matched; `t/160/` → matched; `t/26760/` → matched.
**Verdict:** CONFIRMED_OK

---

## Domain: ATC / Revenue Check Robustness

### ATC-001 — Module-level `atcCheckCount` shared across all Playwright projects — HIGH

> **Plain English:** The ATC sample counter never resets between viewport projects (desktop/tablet/mobile/lighthouse). The desktop project exhausts the 5-product cap first, leaving tablet, mobile, and lighthouse with zero ATC→cart flows and no cart health data for those viewports.

`let atcCheckCount = 0` at module scope in `tests/checks/revenue.ts` is initialized once per worker process and never reset. With 4 projects and `workers: 2`, the counter is exhausted after the first project's 5 products. Subsequent projects in the same worker see `atcCheckCount >= 5` from line 1 and skip all ATC flows silently. *(Also confirmed by ACCUM-005 and CONC-004.)*

**File:** `tests/checks/revenue.ts:15`
**Evidence:**
```ts
let atcCheckCount = 0;          // module scope — never reset between projects
const ATC_SAMPLE_LIMIT = 5;

if (atcCheckCount < ATC_SAMPLE_LIMIT) {   // always false after first project
  atcCheckCount++;
```
**Verdict:** BUG

---

### ATC-002 — `Promise.race` timeout does not cancel in-flight `atcFlow`; ghost bugs written to wrong URL — MEDIUM

> **Plain English:** When the 35-second timeout fires, the outer race resolves but `atcFlow` keeps running in the background. If it later completes, `runCartChecks` writes bugs keyed to whatever page the browser is on — which may already be the next URL in the audit loop.

JavaScript `Promise.race` does not cancel other promises. After the `.catch()` swallows the timeout rejection, `runRevenueCheck` returns and the loop advances to the next URL. If `atcFlow` later resolves (the page finally loads `/cart`), `runCartChecks` fires against `page.url()` which now belongs to the next URL iteration.

**File:** `tests/checks/revenue.ts:57`
**Evidence:**
```ts
await Promise.race([atcFlow(), timeout]).catch(() => {
  // atcFlow still running! page.goto inside atcFlow has its own 30s timeout
});
// Loop advances — page.url() now reflects next URL
```
**Verdict:** BUG

---

### ATC-003 — `cartUrl` constructed from `page.url()` after ATC click may reflect mid-navigation URL — MEDIUM

> **Plain English:** The cart URL is built from `page.url()` two seconds after clicking ATC. If the click triggers a navigation (e.g., redirect to a subscription landing page), `page.url()` has already changed and the cart URL is constructed from the wrong host.

`await atc.click()` then `await page.waitForTimeout(2000)` then `new URL('/cart', page.url())`. On Shopify/Recharge configurations that navigate on ATC, `page.url()` after 2s may be on a checkout or subscription host, producing an invalid cart URL like `https://checkout.ryzesuperfoods.com/cart`.

**File:** `tests/checks/revenue.ts:53`
**Evidence:**
```ts
await atc.click();                                           // may trigger navigation
await page.waitForTimeout(2000);                             // page.url() may have changed
const cartUrl = new URL('/cart', page.url()).toString();     // wrong base if navigated
```
**Verdict:** BUG

---

### ATC-004 — `runCartChecks` called twice for `/cart` URLs from URL list — LOW

> **Plain English:** The `/cart` URL appears in the audit URL list AND is navigated to by the ATC flow. Cart checks run twice on the same page, producing duplicate bugs (mitigated by dedup, but inflating raw counts).

`crawl.spec.ts` line 86 calls `runRevenueCheck` when `url.includes('/cart')`. This hits the `url.includes('/cart')` branch in `revenue.ts` (line 66) calling `runCartChecks` directly. Separately, the ATC flow also calls `runCartChecks` when navigating from a product page. Both sets of cart bugs are written to bugs.jsonl.

**File:** `tests/checks/revenue.ts:66`
**Evidence:**
```ts
// crawl.spec.ts: runRevenueCheck called for /cart URL from URL list
// revenue.ts line 66: url.includes('/cart') -> runCartChecks (direct visit)
// revenue.ts line 55: runCartChecks (via ATC flow) — same page, same checks
```
**Verdict:** DESIGN_FLAW

---

### ATC-005 — `atcCheckCount` incremented before ATC flow succeeds, burning slots on failed clicks — LOW

> **Plain English:** The counter is incremented before the click even happens. If the ATC button throws immediately (stale element, timeout), the slot is consumed and not recovered. All 5 slots can be consumed by failures, leaving the audit with zero successful cart validations.

`atcCheckCount++` at line 49 runs before `atcFlow()` executes. If `atc.click()` throws or the timeout fires, the counter has already been incremented with no decrement on the catch path.

**File:** `tests/checks/revenue.ts:49`
**Evidence:**
```ts
if (atcCheckCount < ATC_SAMPLE_LIMIT) {
  atcCheckCount++;                    // burned before any click
  const atcFlow = async () => {
    await atc.click();                // may throw
  };
  await Promise.race([atcFlow(), timeout]).catch(() => {
    // no decrement here
  });
}
```
**Verdict:** BUG

---

### ATC-006 — Checkout button selector matches `<a>` tags which are always `isEnabled()` — INFO

> **Plain English:** The checkout check uses `isEnabled()` on a locator that also matches anchor tags. Playwright always reports `<a>` elements as enabled (unless `aria-disabled=true`), so a visually disabled checkout link passes the check silently.

`page.locator('button[name="checkout"], a[href*="checkout"]').first().isEnabled()` — if the `<a>` variant matches first, `isEnabled()` returns `true` unconditionally, bypassing the real disabled checkout button.

**File:** `tests/checks/revenue.ts:84`
**Evidence:**
```ts
const checkoutBtn = page.locator('button[name="checkout"], a[href*="checkout"]').first();
const checkoutEnabled = await checkoutBtn.isEnabled().catch(() => false);
// <a> elements always return true from isEnabled()
```
**Verdict:** BUG

---

### ATC-007 — `p-limit(2)` is no-op with a single `page` object — INFO

> **Plain English:** The for-loop awaits each `limit()` call individually, making execution serial. The concurrency window never opens. Even if it did, a single Playwright Page can't be navigated concurrently.

`for (const url of allUrls) { await limit(async () => { ... }) }` — `await` on the `limit()` call prevents any parallelism. p-limit provides no benefit and creates a misleading impression of concurrency.

**File:** `tests/crawl.spec.ts:17`
**Evidence:** `for (const url of allUrls) { await limit(async () => { page.goto(url) }) }` — sequential by construction.
**Verdict:** DESIGN_FLAW

---

### ATC-008 — `new URL('/cart', page.url())` correct in normal (no-navigation) case — INFO

> **Plain English:** When the ATC click opens a drawer without navigating, `page.url()` still points to the product URL and `new URL('/cart', ...)` correctly produces the cart URL for both `www` and `shop` hosts.

The WHATWG URL spec uses only the origin (scheme+host+port) from the base, ignoring the path, so deep product paths produce the correct cart URL.

**File:** `tests/checks/revenue.ts:53`
**Evidence:** `new URL('/cart', 'https://www.ryzesuperfoods.com/products/mushroom-coffee')` → `https://www.ryzesuperfoods.com/cart` ✓
**Verdict:** CONFIRMED_OK

---

## Domain: Deduplication Fingerprint Correctness

### DEDUP-001 — `shouldMerge()` is dead code — fuzzy Hamming merge never executes — HIGH

> **Plain English:** The fuzzy visual merge function is defined and exported but never called. Visually similar elements that differ by a few pixels in their perceptual hash are never merged — every distinct hash gets its own record.

`deduplicateBugs()` builds only an exact-fingerprint Map and never calls `shouldMerge()`. Project-wide grep finds zero call sites for `shouldMerge` outside its definition and a planning doc reference.

**File:** `src/dedupe/fingerprint.ts:65`
**Evidence:** `grep -rn 'shouldMerge'` → only `src/dedupe/fingerprint.ts:43` (definition) and planning doc. No call site in `src/`, `tests/`, or `scripts/`.
**Verdict:** BUG

---

### DEDUP-002 — `getSectionAnchor()` is dead code — `sectionAnchor` always falls back to `'document'` — HIGH

> **Plain English:** The function that identifies which Shopify section a bug belongs to is never called. Every bug in every section of every page uses the same anchor string `'document'`, so all same-rule violations across the entire site collapse into one record regardless of page location.

`getSectionAnchor()` is exported from `src/dedupe/selector-path.ts` but has zero imports or call sites. `sectionAnchor` is not a field on `BugInstance`, so every `bugs.add()` call omits it. At dedup time, `inst.sectionAnchor ?? 'document'` always evaluates to `'document'`.

**File:** `src/dedupe/selector-path.ts:8`
**Evidence:** `grep -rn 'getSectionAnchor'` → only definition. `grep -rn 'sectionAnchor'` in `tests/` → zero results.
**Verdict:** BUG

---

### DEDUP-003 — `dHashHex` truncated to 16 binary bits instead of 64, causing false-positive collision risk — HIGH

> **Plain English:** The code slices the perceptual hash to 16 characters expecting 16 hex digits (64 bits), but sharp-phash returns a 64-character binary string of `'0'` and `'1'`. Only 16 bits are actually used, causing a ~3.6× collision rate increase.

`sharp-phash` returns a 64-char binary string (each character is `'0'` or `'1'`). `computeFingerprint()` does `dHashHex.slice(0, 16)`, treating it as hex (expecting 64 bits), but gets only 16 bits. Birthday paradox with 687 screenshots and 65,536 possible values predicts ~3.6 false-positive collisions per run.

**File:** `src/dedupe/fingerprint.ts:37`
**Evidence:**
```ts
// sharp-phash output: '0110100101...' (64 binary chars)
const hashPart = dHashHex ? dHashHex.slice(0, 16) : 'nohash';
// Slices 16 binary chars = 16 bits, not 64
```
**Verdict:** BUG

---

### DEDUP-004 — `dHash` and `sectionAnchor` fields absent from `BugInstance` — entire perceptual-hash pipeline disconnected — MEDIUM

> **Plain English:** Neither `dHash` nor `sectionAnchor` appear on `BugInstance`, and no check module ever passes them to `bugs.add()`. The perceptual hash infrastructure (computeHash, hammingDistance, shouldMerge) and the DOM section walker are entirely disconnected from bug collection.

`BugInstance` in `src/types.ts` has no `dHash` or `sectionAnchor` fields. All 11 `bugs.add()` call sites omit them. The extended type `BugInstance & { dHash?: string; sectionAnchor?: string }` in `deduplicateBugs()` is a placeholder no runtime value ever satisfies with non-undefined values.

**File:** `src/types.ts:15`
**Evidence:** `grep -rn 'dHash'` in `tests/` → zero results. `grep -rn 'computeHash'` in `tests/`, `scripts/` → zero results.
**Verdict:** BUG

---

### DEDUP-005 — All `network:404` bugs collapse into one record, losing all but the first broken resource URL — HIGH

> **Plain English:** Every `network:404` message normalizes to `'HTTP 404: URL'` after stripping the resource URL. All broken resources — missing images, CSS, fonts — share one fingerprint and the report shows a single 404 entry with only the first resource URL preserved.

`normalizeMessage()` strips all URLs with `.replace(/https?:\/\/[^\s,)'"]+/g, 'URL')`. The message `'HTTP 404: https://cdn.shopify.com/.../style.css'` and `'HTTP 404: https://www.ryzesuperfoods.com/images/hero.jpg'` both normalize to `'HTTP 404: URL'`. With `sectionAnchor='document'` and `dHash='nohash'`, all share one SHA1 fingerprint. The `description` field on the merged record retains only the first instance's message; all others are silently discarded.

**File:** `src/dedupe/fingerprint.ts:9`
**Evidence:** `normalizeMessage('HTTP 404: https://cdn.shopify.com/.../main.css') === normalizeMessage('HTTP 404: https://www.ryzesuperfoods.com/hero.jpg') === 'HTTP 404: URL'`
**Verdict:** DESIGN_FLAW

---

### DEDUP-006 — `hammingDistance` receives binary strings but is documented for hex — MEDIUM

> **Plain English:** The function works correctly by coincidence (binary `'0'`/`'1'` parse as 0/1 in base-16 too), but its contract says hex and its input is binary. A future caller passing a real hex string would get wrong results silently.

`parseInt(a[i]!, 16)` for binary chars `'0'`/`'1'` returns 0 or 1 — numerically identical to the binary comparison needed. But the function signature, comment, and name say "hex hash strings." Any true hex input would produce incorrect bit-count semantics.

**File:** `src/dedupe/perceptual-hash.ts:19`
**Evidence:**
```ts
// Comment: 'Compute Hamming distance between two hex hash strings.'
// Actual input: sharp-phash 64-char binary string '0110...'
// parseInt('a', 16) = 10 → would count 3 bits (1010), not 1
```
**Verdict:** DESIGN_FLAW

---

### DEDUP-007 — `getSectionAnchor()` class selector uses `[class^=]` which fails on multi-valued class attributes — LOW

> **Plain English:** The section selector `element[class^=section-hero]` only matches elements where the class attribute *starts with* that value. An element with `class="container section-hero"` would not match. (Latent — function is never called.)

`[class^=value]` requires the attribute string to begin with `value`. Multi-class elements like `<div class="wrapper section-hero">` would not match; `[class~=value]` is the correct selector.

**File:** `src/dedupe/selector-path.ts:26`
**Evidence:**
```ts
const cls = Array.from(node.classList).find((c) => /^(section|sec)-/.test(c));
if (cls) return `${node.tagName.toLowerCase()}[class^=${cls}]`;
// Should be [class~=${cls}] for multi-valued class attributes
```
**Verdict:** BUG

---

### DEDUP-008 — `URL_PATTERN` only strips lowercase hex hash suffixes — LOW

> **Plain English:** The regex that normalizes content-hash URL suffixes only matches `[a-f0-9]` (lowercase). An asset with an uppercase hex hash would not be normalized and would create separate fingerprints across pages.

`URL_PATTERN = /\/[a-zA-Z0-9_-]+-[a-f0-9]{8,}(\.\w+)?/g` — the hash portion uses `[a-f0-9]` while the filename portion uses `[a-zA-Z0-9_-]`. Shopify CDN hashes are lowercase in practice, but the inconsistency is unexpected.

**File:** `src/dedupe/fingerprint.ts:5`
**Evidence:** `const URL_PATTERN = /\/[a-zA-Z0-9_-]+-[a-f0-9]{8,}(\.\w+)?/g` — `[a-f0-9]` excludes `A-F`.
**Verdict:** BUG

---

### DEDUP-009 — `axeNodes` field collected on `BugInstance` but discarded at dedup time — INFO

> **Plain English:** The a11y check populates `axeNodes` (CSS selectors for all violated elements) but `BugRecord` has no `axeNodes` field and `deduplicateBugs()` never transfers it. This data is collected and silently thrown away.

`BugInstance.axeNodes?: string[]` is set in `a11y.ts` line 43. `BugRecord` has no such field. The docx report therefore never receives per-element selector information for a11y violations.

**File:** `src/types.ts:35`
**Evidence:** `a11y.ts:43: axeNodes: node.target.map(String)`. `BugRecord` interface: no `axeNodes` field.
**Verdict:** DESIGN_FLAW

---

## Domain: Sitemap Discovery & robots.txt Compliance

### CRAWL-001 — `robots.txt` compliance completely unimplemented — CRITICAL

> **Plain English:** CLAUDE.md lists honoring `robots.txt` as a non-negotiable constraint, but `robots-parser` is never imported or called anywhere in the codebase. Every URL from the sitemap is visited unconditionally, including any paths Shopify disallows for bots.

`robots-parser` is installed in `node_modules` but has zero imports or call sites across all `.ts` files. The JSDoc on `discoverUrls()` says "Caller is responsible for robots.txt filtering" — but both callers (`scripts/crawl.ts` and `tests/crawl.spec.ts`) use the returned URLs directly with no robots.txt check. The audit loop calls `page.goto(url)` for every URL unconditionally.

**File:** `src/crawl/sitemap.ts` (Cross-cutting — both callers)
**Evidence:** `grep -rn 'robots'` across all `.ts` files → one hit: JSDoc comment only. Zero imports, zero `isAllowed()` calls.
**Verdict:** BUG

---

### CRAWL-002 — `loc.includes('sitemap')` drops content URLs containing "sitemap" in their slug — HIGH

> **Plain English:** Any blog article or product whose URL slug contains the word "sitemap" is silently mis-routed as a sub-sitemap fetch, fails to parse as XML, and disappears from the audit entirely.

Line 107: `if (loc.endsWith('.xml') || loc.includes('sitemap'))` matches any URL containing the substring `'sitemap'` case-sensitively. A URL like `https://www.ryzesuperfoods.com/blogs/health/understanding-xml-sitemaps` passes the check, gets queued as an XML fetch, `curlFetch` returns HTML, `parseLocUrls` returns `[]`, and the article is never audited.

**File:** `src/crawl/sitemap.ts:107`
**Evidence:**
```ts
if (loc.endsWith('.xml') || loc.includes('sitemap')) {
  queue.push(loc);  // content URL treated as sitemap index
  continue;
}
```
`'sitemap' in 'https://www.ryzesuperfoods.com/blogs/health/understanding-sitemap-architecture'` → `true`
**Verdict:** BUG

---

### CRAWL-003 — Blog cap counter increments before dedup check, under-sampling unique blogs — MEDIUM

> **Plain English:** If the same blog article URL appears in two sub-sitemaps, the 20-blog cap counter is bumped twice but the URL is only added once. Each duplicate consumes a cap slot, leaving fewer than 20 unique articles in the result.

`blogCounts[host]` is incremented at line 116 before the dedup check at line 120 (`if (!result[category].includes(loc))`). If a URL is already present, the count was already bumped. For N duplicate URLs, up to N fewer unique articles are collected than the intended cap.

**File:** `src/crawl/sitemap.ts:116`
**Evidence:**
```ts
blogCounts[host] = (blogCounts[host] ?? 0) + 1; // bumped
if (blogCounts[host] > BLOG_SAMPLE_LIMIT) continue;
// ...
if (!result[category].includes(loc)) { result[category].push(loc); } // dedup after cap
```
**Verdict:** BUG

---

### CRAWL-004 — Blog index pages with trailing slash mis-categorized as blog articles — LOW

> **Plain English:** `/blogs/health/` splits into 4 segments (including empty trailing segment) and passes the blog-article filter, treating a listing page as an article to audit.

Line 51: `pathname.split('/').length >= 4`. For `/blogs/health/`, `split('/')` → `['', 'blogs', 'health', '']` — length 4 — satisfies the condition. The correct check should verify the 4th segment is non-empty.

**File:** `src/crawl/sitemap.ts:51`
**Evidence:** `'/blogs/health/'.split('/') → ['', 'blogs', 'health', ''], len=4, is_blog=True`
**Verdict:** BUG

---

### CRAWL-005 — `p-limit(2)` is dead code — for-loop `await` makes URL processing serial — LOW

> **Plain English:** The developer imported `p-limit(2)` intending parallelism but immediately awaits each call in the for-loop, making execution strictly serial. The concurrency cap is never reached.

`for (const url of allUrls) { await limit(async () => { ... }) }` — because the loop awaits each `limit()` call before advancing, only one URL is ever in-flight. `pLimit` would only provide real parallelism with `Promise.all(allUrls.map(url => limit(...)))` — but that would also be broken with a single shared `page` object. *(Also identified as CONC-001.)*

**File:** `tests/crawl.spec.ts:17`
**Evidence:** `for (const url of allUrls) { await limit(async () => { ... }); }` — sequential by construction.
**Verdict:** DESIGN_FLAW

---

### CRAWL-006 — `parseLocUrls()` and `categorize()` outside try/catch; three real throw paths exist — LOW

> **Plain English:** The try/catch wraps only `curlFetch`. Three real throw paths outside this guard can crash `discoverUrls()`: a truncated `<?xml` PI tag causes `fast-xml-parser` to throw, deep nesting beyond 100 levels throws, and a relative or empty `<loc>` value causes `new URL()` to throw `Invalid URL`.

Confirmed throw paths: (1) `XMLParser.parse()` throws `"Pi Tag is not closed."` for truncated `<?xml` input. (2) `XMLParser.parse()` throws `"Maximum nested tags exceeded"` (added in fast-xml-parser v4.5.4, within the `^4.5.0` pin range). (3) `new URL(loc)` throws `"Invalid URL"` for relative paths or empty strings in `<loc>` elements. In practice Shopify sitemaps are well-formed, so these paths have never fired — but they are real.

**File:** `src/crawl/sitemap.ts:103`
**Evidence:**
```ts
// try/catch wraps only curlFetch (lines 97-102)
const locs = parseLocUrls(xml);  // can throw on truncated <?xml PI
// ...
const category = categorize(loc); // calls new URL(loc) — throws on relative URLs
```
**Verdict:** DESIGN_FLAW

---

## Domain: Content Check Injection Safety & Scope

### CONTENT-001 — Empty `catch` block silently discards all detected typos — CRITICAL

> **Plain English:** When cspell finds typos it exits with code 1, causing `execSync` to throw. The catch block is completely empty, so the typo words in `err.stdout` are discarded and `bugs.add()` is never called. No content:typo bugs are ever reported.

`execSync` throws a `SpawnError` with exit code 1 when cspell finds misspellings. The thrown error has a `.stdout` property containing the typo words. The comment in the catch block correctly describes the intent ("cspell exits non-zero when typos found — catch and parse stdout") but the implementation does not extract `err.stdout`. The `result` variable in the try block is only assigned on exit 0 (no typos found).

**File:** `tests/checks/content.ts:55`
**Evidence:**
```ts
try {
  const result = execSync(...).trim(); // only set on exit 0 (no typos!)
  if (result) { /* parse */ }          // never reached when typos exist
} catch {
  // cspell exits non-zero when typos found — catch and parse stdout
  // BUG: err.stdout is never read here
} finally {
  unlinkSync(tmpFile);
}
```
**Verdict:** BUG

---

### CONTENT-002 — cspell silently skips `tmpFile` because it is outside the project working directory — CRITICAL

> **Plain English:** cspell v8 does not check files given as absolute paths outside its working directory. The tmpfile in `/var/folders/.../T/` is always outside the project root, so cspell exits with "Files checked: 0" for every invocation — typos are never found even if the catch-block bug were fixed.

`os.tmpdir()` on macOS returns `/var/folders/...` which is outside the project CWD. When cspell is invoked with an absolute path outside its CWD it reports "Files checked: 0, Issues found: 0 in 0 files." and exits 1. Both CONTENT-001 and CONTENT-002 independently guarantee `content:typo` is never reported.

**File:** `tests/checks/content.ts:34`
**Evidence:**
```
execSync('cspell /var/folders/.../ryze-content-1234.txt ...')
→ exit 1, stdout: 'CSpell: Files checked: 0, Issues found: 0 in 0 files.\n'
```
**Verdict:** BUG

---

### CONTENT-003 — TreeWalker includes text inside `<script>`, `<style>`, `aria-hidden`, and `display:none` nodes — HIGH

> **Plain English:** The TreeWalker visits every text node in the document body with no visibility filter, feeding minified JavaScript and CSS to cspell. If the cspell path bugs were fixed, this would generate hundreds of false-positive typo reports from JS identifier names and CSS property values.

`document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT)` with no filter argument visits all descendant Text nodes regardless of parent element type or CSS visibility. It does not skip `<script>` (inline JS), `<style>` (inline CSS), `aria-hidden="true"` elements, or `display:none` containers.

**File:** `tests/checks/content.ts:20`
**Evidence:**
```ts
const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
// No filter function — walks <script>, <style>, hidden elements
```
**Verdict:** BUG

---

### CONTENT-004 — `execSync` blocks the Playwright worker event loop for up to 15 seconds per URL — MEDIUM

> **Plain English:** `execSync` freezes Node.js entirely while cspell runs. The Playwright worker cannot process browser events or CDP messages during this time. Worst-case stall: 229 URLs × 15s = ~57 minutes per viewport project.

`execSync({ timeout: 15_000 })` holds the V8 event loop for the full duration. In a Playwright worker, this prevents browser heartbeats. The idiomatic fix is `execFile` with promisified callbacks.

**File:** `tests/checks/content.ts:38`
**Evidence:**
```ts
const result = execSync(
  `"${CSPELL_BIN}" "${tmpFile}" ... 2>&1`,
  { encoding: 'utf8', stdio: 'pipe', timeout: 15_000 }, // blocks event loop
);
```
**Verdict:** DESIGN_FLAW

---

### CONTENT-005 — `2>&1` merges cspell stderr into stdout, corrupting the typo word list — MEDIUM

> **Plain English:** cspell writes typo words to stdout and its summary to stderr. Merging them with `2>&1` causes the summary line ("CSpell: Files checked: 1, Issues found: 4") to appear in the parsed output as a spurious typo word.

The command ends with `2>&1`. If the catch-block bug (CONTENT-001) were fixed and `err.stdout` were read, the merged output would include the summary line which `split('\n').filter(Boolean)` would parse as a typo word.

**File:** `tests/checks/content.ts:39`
**Evidence:**
```
stdout (merged): 'quck\nbrownn\nCSpell: Files checked: 1, Issues found: 2 in 1 file.\n'
→ ['quck', 'brownn', 'CSpell: Files checked: 1, Issues found: 2 in 1 file.']
```
**Verdict:** BUG

---

### CONTENT-006 — `unlinkSync` in `finally` throws `ENOENT` if `writeFileSync` failed — LOW

> **Plain English:** If `writeFileSync` fails (e.g., disk full), the empty `catch` swallows it, and then `finally` tries to delete a file that was never created, throwing `ENOENT` — a less informative error than the original failure.

`writeFileSync` is the first operation in the try block. If it throws, `catch {}` swallows it, then `finally { unlinkSync(tmpFile) }` runs on a non-existent file. Node.js `unlinkSync` throws `ENOENT` synchronously, which propagates to the caller.

**File:** `tests/checks/content.ts:58`
**Evidence:**
```ts
const tmpFile = join(tmpdir(), ...);
try { writeFileSync(tmpFile, text); ... }
catch { }
finally { unlinkSync(tmpFile); } // ENOENT if writeFileSync never created it
```
**Verdict:** BUG

---

### CONTENT-007 — `Date.now()` tmpfile naming has millisecond collision risk across concurrent workers — LOW

> **Plain English:** Three Playwright viewport projects run simultaneously, each calling `runContentCheck` for the same URL at nearly the same time. Two workers resolving `Date.now()` within the same millisecond write to the same tmpfile, corrupting each other's content.

`join(tmpdir(), \`ryze-content-${Date.now()}.txt\`)` uses millisecond precision. With 3+ concurrent workers and no worker-unique component, two workers can produce identical filenames. The fix is `crypto.randomUUID()` or including the viewport name.

**File:** `tests/checks/content.ts:34`
**Evidence:**
```ts
const tmpFile = join(tmpdir(), `ryze-content-${Date.now()}.txt`);
// Called from desktop, tablet, mobile workers simultaneously
// Date.now() has 1ms precision on macOS
```
**Verdict:** BUG

---

### CONTENT-008 — Unix `unlink` during cspell SIGTERM is safe — INFO

> **Plain English:** When `execSync` times out and kills cspell, the `finally` block deletes the tmpfile while cspell may still be running. On macOS/Linux, POSIX guarantees cspell can still read via its open file descriptor — no data corruption occurs.

POSIX `unlink()` removes the directory entry but preserves the inode while any process holds an open fd. cspell continues reading its already-open fd safely after the name is removed.

**File:** `tests/checks/content.ts:57`
**Evidence:** POSIX spec: unlink removes name, inode persists while open fd exists.
**Verdict:** CONFIRMED_OK

---

## Domain: Bug Accumulation & Multi-Run Data Integrity

### ACCUM-001 — No multi-run detection or warning — `bugs.jsonl` silently accumulates — HIGH

> **Plain English:** Every audit run appends to bugs.jsonl with no way for the report script to know it is reading data from multiple runs. The report presents inflated `instanceCount` values and incorrect totals with no warning.

`report.ts` reads the entire bugs.jsonl unconditionally. Confirmed live: the file contains two distinct runs (2026-05-01T01:42 and 2026-05-01T02:31). No `--since` flag, no run-ID tracking, no `console.warn` when timestamps span multiple sessions.

**File:** `scripts/report.ts:84`
**Evidence:**
```ts
const lines = readFileSync(BUGS_PATH, 'utf8').split('\n').filter(Boolean);
// Reads ALL lines from all runs, unconditionally
```
**Verdict:** DESIGN_FLAW

---

### ACCUM-002 — `totalPages` counts pages-with-bugs, not pages-crawled — 42% undercount confirmed — HIGH

> **Plain English:** The report's "Pages crawled" figure is computed from bug instances, not the actual URL list. Any page with zero actionable bugs after noise filtering is invisible to this count. Live data: 132 vs 229 actual URLs — a 42% undercount.

Line 100: `totalPages: new Set(instances.map((i) => i.url)).size` on the post-noise-filtered subset. The correct approach is to read `output/url-list.json`.

**File:** `scripts/report.ts:100`
**Evidence:**
```ts
totalPages: new Set(instances.map((i) => i.url)).size
// Live result: 132 unique URLs in filtered instances vs 229 in url-list.json
```
**Verdict:** BUG

---

### ACCUM-003 — `lighthouse` Playwright project labels all findings as `'desktop'`, doubling desktop `instanceCount` — HIGH

> **Plain English:** The lighthouse project name contains neither 'tablet' nor 'mobile', so `viewportFromProject()` returns `'desktop'`. Every bug is logged twice under `viewport='desktop'`, making desktop instance counts 2× inflated. Live data confirms this pattern.

`viewportFromProject()` falls through to `return 'desktop'` for the `'lighthouse'` name. Confirmed by live data: axe violations show a desktop:tablet ratio of 669:1 (10,713 vs 16 instances), far exceeding the expected 2–3× ratio. *(Also identified as CONC-005.)*

**File:** `tests/crawl.spec.ts:19`
**Evidence:**
```ts
function viewportFromProject(name: string | undefined): Viewport {
  if (!name) return 'desktop';
  if (name.includes('tablet')) return 'tablet';
  if (name.includes('mobile')) return 'mobile';
  return 'desktop'; // 'lighthouse' falls here — doubles all desktop bugs
}
```
**Verdict:** BUG

---

### ACCUM-004 — `flush()` runs on failed tests before retry, injecting partial-run data — MEDIUM

> **Plain English:** When Playwright retries a failed test (`retries: 1`), the `BugCollector` fixture teardown still flushes to bugs.jsonl before the retry. The retry then writes a second complete set. Both partial and full runs persist in the file.

Playwright fixture teardown always runs regardless of test pass/fail status. `retries: 1` in `playwright.config.ts` means a failed test is retried, but the pre-retry flush has already written. Confirmed by live data: the file contains a 22-minute partial run followed by a 205-minute complete run.

**File:** `tests/fixtures/bug-collector.ts:23`
**Evidence:**
```ts
bugs: async ({ page: _page }, use, testInfo) => {
  const collector = new BugCollector(testInfo);
  await use(collector);
  collector.flush(); // runs even when testInfo.status === 'failed', before retry
}
```
**Verdict:** DESIGN_FLAW

---

### ACCUM-006 — No mechanism to clear or time-bound `bugs.jsonl` — no `clean` script, no `--since` flag — MEDIUM

> **Plain English:** There is no documented or automated way to reset bugs.jsonl before a fresh audit without manually deleting the file. `package.json` has no `clean` script, and `full-audit` does not truncate the file before running.

`BUGS_PATH` is hardcoded with no CLI overrides. `package.json` scripts: `test:crawl`, `test:audit`, `report`, `full-audit` — no `clean` or `reset` target. CLAUDE.md acknowledges the file is append-only but provides no procedural guidance for clearing it.

**File:** `scripts/report.ts:7`
**Evidence:**
```json
"scripts": {
  "test:crawl": "...", "test:audit": "...", "report": "...", "full-audit": "..."
  // No "clean" script
}
```
**Verdict:** MISSING_FEATURE

---

### ACCUM-007 — `instanceCount` conflates four unrelated quantities — semantically meaningless — MEDIUM

> **Plain English:** The report's instance count combines: per-element axe node counts, lighthouse project duplicates, multi-run accumulation, and retry partial flushes. A reader cannot interpret `instanceCount=896` as "896 broken elements" vs "1 element visited 896 times."

`existing.instanceCount++` in `deduplicateBugs()` increments for every matching instance regardless of source. Live data: `axe:button-name` on `oats-products/desktop` shows `instanceCount=896` (224 elements × 4 project-runs).

**File:** `src/dedupe/fingerprint.ts:94`
**Evidence:**
```ts
if (existing) {
  if (!existing.urls.includes(inst.url)) existing.urls.push(inst.url);
  if (!existing.viewports.includes(inst.viewport)) existing.viewports.push(inst.viewport);
  existing.instanceCount++; // conflates elements, runs, projects, retries
}
```
**Verdict:** DESIGN_FLAW

---

### ACCUM-009 — `/full-audit` command uses `pnpm` but project uses `npm` — LOW

> **Plain English:** The `/full-audit` slash command instructs Claude to run `pnpm` commands, but the project has a `package-lock.json` (npm) and no `pnpm-lock.yaml`. Running it literally will fail if pnpm is not installed.

`.claude/commands/full-audit.md` uses `pnpm test:crawl`, `pnpm test:audit`, `pnpm report`. CLAUDE.md uses `npm run` equivalents. `package-lock.json` exists; no `pnpm-lock.yaml`.

**File:** `.claude/commands/full-audit.md:8`
**Evidence:**
```md
1. `pnpm test:crawl` — discover URLs
2. `pnpm test:audit` — run all checks
// CLAUDE.md: npm run test:crawl / npm run test:audit
```
**Verdict:** BUG

---

## Domain: Playwright Concurrency & Listener Isolation

### CONC-002 — `p-limit(2)` on a single `Page` object would corrupt browser state if the `await` were removed — HIGH

> **Plain English:** The `p-limit(2)` wrapper would attempt two concurrent `page.goto()` calls on the same Playwright Page object if the for-loop `await` were ever changed. Concurrent navigations on one page are undefined behavior and would corrupt all check results silently.

`p-limit(2)` is the only safeguard against concurrent access to the shared `page` fixture. Because the loop currently awaits each call individually, this is a latent structural risk — one `await` removal by a future developer enables a hard-to-diagnose concurrency bug.

**File:** `tests/crawl.spec.ts:73`
**Evidence:**
```ts
// Single page fixture shared across all URL iterations
async ({ page, bugs }, testInfo) => {
  // ...
  for (const url of allUrls) {
    await limit(async () => { page.goto(url); runA11yCheck(page, ...) })
  }
}
```
**Verdict:** DESIGN_FLAW

---

### CONC-003 — ATC flow navigates shared `page` to `/cart`, causing content check and screenshot to run on wrong page — HIGH

> **Plain English:** After the ATC flow runs `page.goto('/cart')` and returns, the main loop continues running `runContentCheck` and `takeScreenshot` on the still-cart-page. Product page content is never spell-checked, and the screenshot file named after the product contains cart page content.

`atcFlow()` calls `page.goto(cartUrl)` and leaves the page on `/cart`. Control returns to `crawl.spec.ts` which then calls: (1) `runContentCheck(page, ...)` — `page.url()` returns `/cart`, so cart text is spell-checked against the product URL; (2) `takeScreenshot(page, slug, viewport)` — `slug` is the product slug but `page.screenshot()` captures the cart. The docx report embeds cart screenshots for product bug entries.

**File:** `tests/checks/revenue.ts:54`
**Evidence:**
```ts
// atcFlow navigates to /cart and returns — page stays on /cart
await page.goto(cartUrl, { waitUntil: 'load', timeout: 30_000 });
await runCartChecks(page, bugs, viewport, url);
// Caller (crawl.spec.ts) then runs:
await runContentCheck(page, bugs, viewport); // page is on /cart, not product
await takeScreenshot(page, slug, viewport);  // slug = product, content = cart
```
**Verdict:** BUG

---

### CONC-006 — `appendFileSync` concurrent writes are atomic per line via O_APPEND — INFO

> **Plain English:** With 2 worker processes writing simultaneously, each `appendFileSync` uses `O_APPEND` which POSIX guarantees is atomic per write syscall on local filesystems. Lines from different workers may interleave at the line level but each JSONL line is complete and parseable.

POSIX `O_APPEND` makes the seek-to-end and write atomic. Each `appendFileSync` call writes exactly one JSONL line. `scripts/report.ts` reads by `split('\n')` and parses each line independently — interleaved-but-whole lines are handled correctly.

**File:** `tests/fixtures/bug-collector.ts:24`
**Evidence:** POSIX O_APPEND atomicity guarantee for local filesystems.
**Verdict:** CONFIRMED_OK

---

### CONC-007 — Console/network listener viewport label correctly fixed at attachment time — INFO

> **Plain English:** Listeners are attached once with the viewport label captured in a closure. Since each project worker has a fixed viewport for its entire lifetime, the closed-over value is always correct. No stale-capture bug exists here.

`attachConsoleListeners(page, bugs, viewport)` captures `viewport` at attachment time. `viewport` is constant for the lifetime of a given project's test worker. `page.url()` inside the listener correctly evaluates at event-fire time.

**File:** `tests/crawl.spec.ts:69`
**Evidence:** `viewport` determined once via `viewportFromProject(testInfo.project.name)` and captured in closure — correct for fixed-viewport workers.
**Verdict:** CONFIRMED_OK

---

### CONC-008 — Max concurrent browser contexts is 2 — matches stated constraint — INFO

> **Plain English:** With `workers: 2` and each `@audit` test using a single `page` fixture, at most 2 browser contexts exist simultaneously — satisfying the "max 2 concurrent contexts" constraint.

`workers: 2` → 2 worker processes. Each worker runs one `@audit` test at a time with one `page` fixture. The 4 projects are serialized through the 2-worker pool. `p-limit(2)` within each test is a no-op (see CRAWL-005).

**File:** `playwright.config.ts` (Cross-cutting)
**Evidence:** `workers: 2` × 1 context per test = 2 maximum concurrent contexts.
**Verdict:** CONFIRMED_OK

---

## Prioritized Recommendations

1. **CRITICAL — Fix the content check** — addresses CONTENT-001, CONTENT-002, CONTENT-003, CONTENT-005
   - Change the `catch` block to read `(err as any).stdout` for typo words
   - Write tmpfile inside the project directory or use `file://` URI prefix
   - Add a `NodeFilter` to the TreeWalker that skips `<script>`, `<style>`, and `aria-hidden` elements
   - Remove `2>&1` redirect; capture stderr separately

2. **CRITICAL — Implement robots.txt compliance** — addresses CRAWL-001
   - Import `robots-parser`; fetch `robots.txt` for each host before the audit loop
   - Filter `discoverUrls()` result through `isAllowed(url, 'RyzeQABot/0.1')` before adding to crawl list

3. **HIGH — Fix the lighthouse project to not double-count desktop bugs** — addresses ACCUM-003
   - Either add `'lighthouse'` as an explicit project name case in `viewportFromProject()` or remove the lighthouse project from the `@audit` test grep pattern (add a `@lighthouse` tag)

4. **HIGH — Fix all `network:404` bugs collapsing into one record** — addresses DEDUP-005, NOISE-001
   - Change `normalizeMessage()` to preserve the URL path component (strip only the scheme+host) for `network:404` messages so each broken resource URL generates its own fingerprint
   - Add `/t\?event=` to NOISE_404_URL_PATTERNS

5. **HIGH — Fix ATC flow leaving page on `/cart`** — addresses CONC-003, ATC-002, ATC-003
   - Save `const productPageUrl = page.url()` before `atc.click()`; use it for `cartUrl` construction
   - After `runCartChecks`, navigate back to the product page before returning: `await page.goto(productPageUrl)`
   - To prevent ghost bugs from the floating `atcFlow` promise: use an `AbortController`-style flag to suppress `runCartChecks` after timeout

6. **HIGH — Fix module-level `atcCheckCount`** — addresses ATC-001, ATC-005
   - Move the counter into the test fixture or pass it as an argument; reset it per-project-run
   - Increment only after a successful ATC click (inside `atcFlow` on success, not before)

7. **HIGH — Wire perceptual hash and section anchor into bug collection** — addresses DEDUP-001, DEDUP-002, DEDUP-003, DEDUP-004
   - Add `dHash?: string` and `sectionAnchor?: string` to `BugInstance` interface
   - Call `getSectionAnchor()` in the a11y check and store in each `bugs.add()` call
   - Fix `dHash` encoding: sharp-phash returns binary strings; either convert to hex or update `computeFingerprint` to use the binary string directly (all 64 chars, not 16)
   - Wire `shouldMerge()` into `deduplicateBugs()` for a fallback fuzzy-merge pass

8. **MEDIUM — Fix `totalPages` metric** — addresses ACCUM-002
   - Read `output/url-list.json` in `scripts/report.ts` and use `Object.values(urlList).flat().length` for `totalPages`

9. **MEDIUM — Add `clean` script and multi-run detection** — addresses ACCUM-001, ACCUM-006
   - Add `"clean": "rm -f data/bugs.jsonl"` to `package.json` scripts
   - Update `full-audit` to `npm run clean && npm run test:crawl && npm run test:audit && npm run report`
   - Add a `console.warn` in `scripts/report.ts` when timestamps in bugs.jsonl span more than 2 hours

10. **MEDIUM — Fix cspell tmpfile collision and event-loop blocking** — addresses CONTENT-007, CONTENT-004
    - Replace `Date.now()` with `crypto.randomUUID()` in the tmpfile name
    - Replace `execSync` with `execFileAsync` (promisified) to free the event loop

11. **LOW — Sync noise host lists and Edgemesh patterns** — addresses NOISE-002, NOISE-003, NOISE-004, NOISE-005
    - Move NOISE_HOSTS and NOISE_URL_PATTERNS to a shared `src/noise-config.ts` imported by both `network.ts` and `report.ts`
    - Add `myshopify.com` to both lists
    - Rename `NOISE_404_URL_PATTERNS` to `NOISE_URL_PATTERNS` and apply to all `network:4xx` ruleIds

12. **LOW — Fix `sitemap` substring check** — addresses CRAWL-002, CRAWL-003, CRAWL-004
    - Change `loc.includes('sitemap')` to `new URL(loc).pathname.endsWith('.xml')` anchored on the pathname
    - Move `blogCounts[host]++` to after the dedup check
    - Add `pathname.split('/')[3]?.length > 0` guard for blog article categorization

13. **LOW — Fix `full-audit.md` to use `npm`** — addresses ACCUM-009
    - Replace `pnpm` with `npm run` throughout `.claude/commands/full-audit.md`

14. **LOW — Add error boundaries to `discoverUrls()`** — addresses CRAWL-006
    - Wrap `parseLocUrls(xml)` in a try/catch with `console.warn` and `continue`
    - Guard `categorize(loc)` with `URL.canParse(loc)` before calling `new URL(loc)`

15. **LOW — Fix `[class^=]` selector and binary/hex mismatch** — addresses DEDUP-007, DEDUP-006, DEDUP-008
    - Change `[class^=${cls}]` to `.${cls}` in `getSectionAnchor()`
    - Fix `hammingDistance` jsdoc to say "binary string" or convert input to hex before the function
    - Change `[a-f0-9]` to `[a-fA-F0-9]` in `URL_PATTERN`
